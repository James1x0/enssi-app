import { countArray } from 'enssi-app/helpers/count-array';

module('CountArrayHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = countArray(42);
  ok(result);
});
